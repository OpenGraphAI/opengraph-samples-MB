# Contributing an Example

To add a new example to this repo, submit a PR that includes:

1. **Source data** — the raw input used to build the graph
2. **`graph.json`** — the produced graph output
3. **`README.md`** — a short write-up explaining what the example demonstrates and how to run it

Place these in a new folder under `examples/your-example-name/`.

